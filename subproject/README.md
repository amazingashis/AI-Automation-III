# Subproject

This subproject allows you to upload a domain model, data dictionary, and source files, then generates scripts using LLMs based on the provided files.

## Usage
- Place your files in the `uploads/` folder.
- Run `main.py` to generate scripts in the `scripts/` folder.
